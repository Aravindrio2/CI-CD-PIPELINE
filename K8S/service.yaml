apiVersion: v1
kind: Service
metadata:
  name: ci-cdpipelines
spec:
  type: NodePort
  selector:
    app: ci-cdpipelines
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000
